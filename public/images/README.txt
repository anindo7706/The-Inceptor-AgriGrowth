Drop the hero photograph here as:

  hero-farmer.jpg

What works: a farmer or field worker in a crop field, shot wide, warm light
(golden hour), with room on one side for the headline to sit. Landscape,
at least 1600px wide.

The login and marketing hero reference it as /images/hero-farmer.jpg and
layer a dark gradient scrim over it. If the file is absent the scrim's
gradient shows on its own, so a missing image degrades cleanly rather than
breaking the page.
