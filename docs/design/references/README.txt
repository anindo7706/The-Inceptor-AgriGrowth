Drop reference images here.

Name them so the filename says what the image is FOR, not where it came from:

  buyer-dashboard-layout.png
  worker-job-card.jpg
  color-palette.png
  map-land-selection.png
  inspection-form-mobile.png

Then reference them from ../../../DESIGN.md so the intent is recorded alongside the image.
